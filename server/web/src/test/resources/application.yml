server:
  port: 8080

spring:
  r2dbc:
    url: r2dbc:h2:mem:///database;DATABASE_TO_UPPER=false

  output:
    ansi:
      enabled: ALWAYS

graphql:
  packages:
    - com.lorenzoog.diekeditora.domain
    - com.lorenzoog.diekeditora.web
    - graphql.relay
