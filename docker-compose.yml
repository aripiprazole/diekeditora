version: '3'

services:
  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/var/backend
    env_file:
      - backend/.env
    networks:
      - diekeditora

  frontend:
    build:
      context: ./frontend
    volumes:
      - ./frontend:/var/www
    env_file:
      - frontend/.env
    ports:
      - ${PORT:-3000}:3000
    networks:
      - diekeditora

  postgres:
    image: postgres:13.2
    restart: always
    env_file:
      - backend/.env
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - ${DB_PORT:-5432}:5432
    volumes:
      - postgres:/var/lib/postgres
    networks:
      - diekeditora

networks:
  diekeditora:
    driver: bridge

volumes:
  postgres:
    driver: local
